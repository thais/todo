.row-fluid
  .span10.offset1
    .hero-unit.text-center
      %h1
        Todo
      %p
        Welcome to the tutorial Todo
        .container
          - if @tasks.empty?
            %span.text-warning There are no tasks!
          - else
            %table.table.table-hover.table-bordered
              %thread
                %tr
                  %th Title
                  %th Created at
                  %th Completed
            %tbody
              - @tasks.each do |task|
                %tr
                  %td
                    %strong= task.title
                  %td.text-info= task.created_at
                  %td.text-success= task.completed

